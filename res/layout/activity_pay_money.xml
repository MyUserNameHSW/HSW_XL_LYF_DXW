<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PayMoneyActivity" >

    <RelativeLayout
        android:id="@+id/r2"
        style="@style/mw"
        android:background="@color/themeColor" >

        <ImageView
            android:id="@+id/pmoney_back"
            style="@style/topbarsize"
            android:src="@drawable/lyy_top_back" />

        <TextView
            android:id="@+id/pmoney_text"
            style="@style/ww"
            android:layout_centerInParent="true"
            android:text="评价商品"
            android:textColor="#ffffff"
            android:textSize="20sp" />
    </RelativeLayout>

    <EditText
        android:id="@+id/product_assess_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/r2"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/two_radius"
        android:enabled="true"
        android:gravity="top"
        android:inputType="textMultiLine"
        android:maxLines="5"
        android:minLines="3"
        android:scrollbars="vertical" >
    </EditText>

    <Button
        android:id="@+id/cassss_submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/product_assess_text"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/btn_login_bg"
        android:gravity="center"
        android:text="提交评价"
        android:textColor="@color/white" />

</RelativeLayout>